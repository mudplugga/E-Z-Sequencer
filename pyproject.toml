[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "retlu-ez-scheduler"
version = "0.1.0"
description = "Beginner-friendly mission planning engine for RETLU"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
